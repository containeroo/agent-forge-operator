---
version: 2
project_name: agent-forge-operator
builds:
  - id: manager
    binary: manager
    main: ./cmd/main.go
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.Version={{ .Tag }}
dockers_v2:
  - images:
      - ghcr.io/containeroo/agent-forge-operator
      - containeroo/agent-forge-operator
    tags:
      - "{{ .Tag }}"
      - latest
    dockerfile: Dockerfile.goreleaser
    ids:
      - manager
    platforms:
      - linux/amd64
      - linux/arm64
    build_args:
      GOVC_VERSION: v0.54.1
    flags:
      - --pull
      - --cache-from=type=gha,scope=agent-forge-operator
      - --cache-to=type=gha,mode=max,scope=agent-forge-operator
    labels:
      org.opencontainers.image.title: "{{ .ProjectName }}"
      org.opencontainers.image.description: "{{ .ProjectName }}"
      org.opencontainers.image.url: https://github.com/containeroo/agent-forge-operator
      org.opencontainers.image.source: https://github.com/containeroo/agent-forge-operator
      org.opencontainers.image.version: "{{ .Version }}"
      org.opencontainers.image.created: '{{ time "2006-01-02T15:04:05Z07:00" }}'
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.licenses: Apache License 2.0
release:
  extra_files:
    - glob: ./crds.yaml
  prerelease: auto
